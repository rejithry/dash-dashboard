Create a project for a dashboard app. With the below features:
- Users should be able to create/edit and view dashboards.
- There should be a new button to create a new dashboard.
- There should be a way to see the list of dashboards, and they should be clickable to view the dashboard.
- The dashboard should be a grid of widgets, each of which should show a chart or table.
- The widgets should be able to be dragged and dropped to reorder them.
- The widgets should be able to be resized.
- The widgets should be able to be deleted.
- Each widget should be able to be edited to change the chart or table type.
- Each chart type should have a set of options that can be configured.
- Preferably use google charts for the charts. 
- Each chart is backed by a database query or dummy data.
- There should be a way to add a new database connection.
- There shuld be a way to edit the database connection.
- There should be a way to delete the database connection.
- The should be way to add/edit/delete widgets.
-  There should be a way to edit the query for the widget.

-- Ask me any clarifying questions.
Make a plan before you start coding.
Store the plan in a file called CLAUDE.md
Use react/nodejs preferably.
Push the code to a github repository and share the link with me.

